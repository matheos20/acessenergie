<form method="post" action="{{ path('gaz_delete', {'id': gaz.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gaz.id) }}">
    <button class="btn">Delete</button>
</form>
